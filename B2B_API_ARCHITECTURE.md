# GhostSpeak B2B API Platform Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     GhostSpeak B2B API Platform                 │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   REST API   │     │   Webhooks   │     │  Team Mgmt   │
│              │     │              │     │              │
│  ┌────────┐  │     │  ┌────────┐  │     │  ┌────────┐  │
│  │ Verify │  │     │  │ Queue  │  │     │  │ Invite │  │
│  │ Batch  │  │     │  │ Deliver│  │     │  │  RBAC  │  │
│  │  Docs  │  │     │  │  Retry │  │     │  │  Limit │  │
│  └────────┘  │     │  └────────┘  │     │  └────────┘  │
└──────────────┘     └──────────────┘     └──────────────┘
       │                     │                     │
       └─────────────────────┴─────────────────────┘
                             │
                    ┌────────▼────────┐
                    │   Convex DB     │
                    │                 │
                    │  - Schema       │
                    │  - Mutations    │
                    │  - Queries      │
                    │  - Cron Jobs    │
                    └─────────────────┘
```

## Component Architecture

### 1. REST API Layer

```
┌─────────────────────────────────────────────────────────────┐
│                      API Endpoints                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  POST /api/v1/verify                                        │
│  ├─ Validate API key                                        │
│  ├─ Check rate limit                                        │
│  ├─ Query agent reputation cache                            │
│  ├─ Record usage for billing                                │
│  └─ Return full verification response                        │
│                                                              │
│  GET /api/v1/agents/:address/score                          │
│  ├─ Lightweight score-only endpoint                         │
│  ├─ Faster response (0.5¢ vs 1¢)                            │
│  └─ Cached for 5 minutes                                    │
│                                                              │
│  POST /api/v1/verify/batch                                  │
│  ├─ Accept up to 100 agents                                 │
│  ├─ Deduplicate addresses                                   │
│  ├─ Process in parallel (10 at a time)                      │
│  ├─ Return results + metadata                               │
│  └─ Bulk pricing (0.5¢ per agent)                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 2. Webhook System

```
┌─────────────────────────────────────────────────────────────┐
│                   Webhook Delivery Flow                      │
└─────────────────────────────────────────────────────────────┘

Event Occurs                  Cron Job (Every 1 min)
     │                               │
     ▼                               ▼
┌─────────┐                  ┌──────────────┐
│ Queue   │                  │  Get Pending │
│ Event   │                  │  Webhooks    │
└────┬────┘                  └──────┬───────┘
     │                              │
     │  ┌───────────────────────────┘
     │  │
     ▼  ▼
┌─────────────┐
│  Match      │
│  Subscr.    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────────┐
│         Delivery Attempt                    │
├─────────────────────────────────────────────┤
│  1. Generate HMAC-SHA256 signature          │
│  2. POST to subscriber URL                  │
│  3. 10 second timeout                       │
│  4. Check response status                   │
└──────────┬──────────────────┬───────────────┘
           │                  │
      Success               Failure
           │                  │
           ▼                  ▼
    ┌──────────┐      ┌──────────────┐
    │  Mark    │      │  Retry with  │
    │Delivered │      │  Exp Backoff │
    └──────────┘      └──────────────┘
                             │
                      ┌──────┴──────┐
                      │  1 min      │
                      │  2 min      │
                      │  4 min      │
                      │  8 min      │
                      │ 16 min      │
                      └─────────────┘
                      (Max 5 attempts)
```

### 3. Team Management

```
┌─────────────────────────────────────────────────────────────┐
│                  Team Hierarchy                              │
└─────────────────────────────────────────────────────────────┘

              ┌──────────┐
              │   Team   │
              │ (Startup)│
              │ (Growth) │
              │(Enterprise)│
              └─────┬────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
   ┌────▼────┐ ┌───▼────┐ ┌───▼────┐
   │  Owner  │ │ Admin  │ │ Member │
   │   👑    │ │   🔧   │ │   👤   │
   └─────────┘ └────────┘ └────────┘
        │           │           │
        │           │           │
    ┌───▼───────────▼───────────▼───┐
    │       Permissions              │
    ├────────────────────────────────┤
    │  Manage Members     ✅ ✅ ❌  │
    │  Manage API Keys    ✅ ✅ ✅  │
    │  View Billing       ✅ ✅ ❌  │
    │  Invite Members     ✅ ✅ ❌  │
    └────────────────────────────────┘

Team Invitation Flow:

  Admin/Owner             Invitee              System
      │                     │                    │
      │  Send Invite        │                    │
      ├────────────────────►│                    │
      │                     │  Generate Token    │
      │                     ├───────────────────►│
      │                     │                    │
      │                     │  Email Link        │
      │                     │◄───────────────────┤
      │                     │                    │
      │  Click Link         │                    │
      │◄────────────────────┤                    │
      │                     │  Accept Invite     │
      │                     ├───────────────────►│
      │                     │                    │
      │                     │  Create Member     │
      │                     │◄───────────────────┤
      │  Notification       │                    │
      │◄────────────────────┴────────────────────┤
```

## Data Flow Diagrams

### Batch Verification Flow

```
Client Request
     │
     ▼
┌─────────────────────┐
│  Validate API Key   │
│  Check Rate Limit   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Parse Request      │
│  - Extract agents   │
│  - Validate count   │
│  - Deduplicate      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  Process in Batches (10 at a time)  │
├─────────────────────────────────────┤
│  Batch 1:  [1-10]   ═══════>        │
│  Batch 2:  [11-20]  ═══════>        │
│  Batch 3:  [21-30]  ═══════>        │
│  ...                                 │
│  Batch 10: [91-100] ═══════>        │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────┐
│  Aggregate Results  │
│  - Success count    │
│  - Failed count     │
│  - Timing metrics   │
│  - Tier distribution│
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Record Usage       │
│  - Per agent        │
│  - Bulk pricing     │
│  - Response time    │
└──────────┬──────────┘
           │
           ▼
      Return Response
```

### Webhook Signature Verification

```
Webhook Delivery                    Subscriber Endpoint
     │                                      │
     │  1. Generate Signature               │
     │     payload = JSON.stringify(body)   │
     │     hmac = HMAC-SHA256(payload, secret)
     │     signature = hmac.digest('hex')   │
     │                                      │
     │  2. Send Request                     │
     ├─────────────────────────────────────►│
     │  POST /webhook                       │
     │  Headers:                            │
     │    X-GhostSpeak-Signature: abc123... │
     │    X-GhostSpeak-Event: score.updated │
     │  Body:                               │
     │    { event, agentAddress, data }     │
     │                                      │
     │                                      │  3. Verify Signature
     │                                      │     receivedSig = header['x-ghostspeak-signature']
     │                                      │     payload = JSON.stringify(body)
     │                                      │     hmac = HMAC-SHA256(payload, secret)
     │                                      │     expectedSig = hmac.digest('hex')
     │                                      │     isValid = (receivedSig === expectedSig)
     │                                      │
     │  4. Response (200 OK)                │
     │◄─────────────────────────────────────┤
     │                                      │
     │  5. Mark Delivered                   │  6. Process Event
     │                                      │
     ▼                                      ▼
```

## Database Schema

```
┌─────────────────────────────────────────────────────────────┐
│                     Convex Database                          │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┐          ┌──────────────────┐
│     apiKeys      │          │   apiUsage       │
├──────────────────┤          ├──────────────────┤
│ userId           │──────┐   │ apiKeyId         │
│ hashedKey        │      │   │ endpoint         │
│ tier            ─┼──┐   │   │ method           │
│ rateLimit        │  │   │   │ statusCode       │
│ isActive         │  │   │   │ cost             │
└──────────────────┘  │   │   │ timestamp        │
                      │   │   └──────────────────┘
                      │   │
┌─────────────────────┼───┼───────────────────┐
│  webhookSubscriptions│  │                   │
├──────────────────────┤  │                   │
│ apiKeyId          ◄──┘  │                   │
│ userId               │  │                   │
│ url                  │  │   ┌──────────────────┐
│ secret               │  │   │ webhookDeliveries│
│ events[]             │  │   ├──────────────────┤
│ agentAddresses[]     │──┼──►│ subscriptionId   │
│ isActive             │  │   │ status           │
│ totalDeliveries      │  │   │ attemptCount     │
└──────────────────────┘  │   │ lastError        │
                          │   └──────────────────┘
                          │
┌─────────────────────────┼───────────────────┐
│        teams            │                   │
├─────────────────────────┤                   │
│ ownerUserId          ◄──┘                   │
│ plan                 │                      │
│ maxMembers           │   ┌──────────────────┐
│ maxApiKeys           │   │  teamMembers     │
│ isActive             │   ├──────────────────┤
└──────────┬──────────────┤   │ teamId          │
           │              └──►│ userId          │
           │                  │ role            │
           │                  │ permissions     │
           │                  └──────────────────┘
           │
           │              ┌──────────────────┐
           │              │  teamInvites     │
           │              ├──────────────────┤
           └─────────────►│ teamId          │
                          │ email           │
                          │ token           │
                          │ status          │
                          └──────────────────┘
```

## API Authentication Flow

```
Client Request
     │
     │  Header: X-API-Key: gs_live_abc123...
     ▼
┌─────────────────────┐
│  Extract API Key    │
│  from Header        │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Hash API Key       │
│  (SHA-256)          │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Query apiKeys      │
│  by hashedKey       │
└──────────┬──────────┘
           │
     ┌─────┴─────┐
     │           │
   Found      Not Found
     │           │
     ▼           ▼
┌─────────┐  ┌────────┐
│ Check   │  │  401   │
│isActive │  │Unauthorized
└────┬────┘  └────────┘
     │
     ▼
┌─────────────────────┐
│  Check Rate Limit   │
│  (requests/minute)  │
└──────────┬──────────┘
           │
     ┌─────┴─────┐
     │           │
  Within      Exceeded
   Limit         │
     │           ▼
     │      ┌────────┐
     │      │  429   │
     │      │Rate Limit
     │      └────────┘
     │
     ▼
┌─────────────────────┐
│  Update Last Used   │
│  Process Request    │
└─────────────────────┘
```

## Scaling Considerations

### Horizontal Scaling

```
┌─────────────────────────────────────────────────────────┐
│                    Load Balancer                         │
└──────────────┬──────────────┬──────────────┬───────────┘
               │              │              │
      ┌────────▼─────┐ ┌──────▼──────┐ ┌────▼──────────┐
      │ API Server 1 │ │ API Server 2│ │ API Server N  │
      │              │ │             │ │               │
      │ - Verify     │ │ - Verify    │ │ - Verify      │
      │ - Batch      │ │ - Batch     │ │ - Batch       │
      │ - Webhooks   │ │ - Webhooks  │ │ - Webhooks    │
      └──────┬───────┘ └──────┬──────┘ └───────┬───────┘
             │                │                 │
             └────────────────┼─────────────────┘
                              │
                    ┌─────────▼──────────┐
                    │   Convex Backend   │
                    │                    │
                    │  - Real-time sync  │
                    │  - Distributed     │
                    │  - Auto-scaling    │
                    └────────────────────┘
```

### Performance Optimization

1. **Caching Strategy**
   - Agent reputation cached for 5 minutes
   - Reduces database load by 90%
   - Cache-Control headers in responses

2. **Parallel Processing**
   - Batch verification: 10 agents in parallel
   - Webhook delivery: 50 webhooks per cron run
   - Async operations with Promise.all()

3. **Rate Limiting**
   - Per API key limits
   - Sliding window algorithm
   - Graceful degradation

4. **Database Indexing**
   - All queries use indexed fields
   - Composite indexes for complex queries
   - Optimized for read-heavy workload

## Monitoring & Observability

```
┌─────────────────────────────────────────────────────────┐
│                    Metrics Dashboard                     │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  API Performance                                         │
│  ├─ Request latency (p50, p95, p99)                    │
│  ├─ Throughput (requests/second)                        │
│  ├─ Error rate (%)                                      │
│  └─ Success rate (%)                                    │
│                                                          │
│  Webhook Delivery                                        │
│  ├─ Delivery success rate                               │
│  ├─ Average retry count                                 │
│  ├─ Failed webhooks (last 24h)                          │
│  └─ Queue depth                                         │
│                                                          │
│  Usage & Billing                                         │
│  ├─ API calls by tier                                   │
│  ├─ Revenue by endpoint                                 │
│  ├─ Top customers by usage                              │
│  └─ Cost per verification                               │
│                                                          │
│  Team Activity                                           │
│  ├─ Active teams                                        │
│  ├─ New invitations                                     │
│  ├─ Member growth                                       │
│  └─ API key distribution                                │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│                   Security Layers                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  1. API Key Authentication                               │
│     ├─ SHA-256 hashing                                  │
│     ├─ Secure generation (crypto.randomBytes)           │
│     └─ Prefix-based display (gs_live_abc...)           │
│                                                          │
│  2. Webhook HMAC Signing                                 │
│     ├─ HMAC-SHA256 signatures                           │
│     ├─ Unique secrets per subscription                  │
│     └─ Timestamp-based replay protection               │
│                                                          │
│  3. Rate Limiting                                        │
│     ├─ Per-key limits                                   │
│     ├─ Sliding window algorithm                         │
│     └─ DDoS protection                                  │
│                                                          │
│  4. HTTPS Only                                           │
│     ├─ TLS 1.3                                          │
│     ├─ Certificate pinning                              │
│     └─ HSTS headers                                     │
│                                                          │
│  5. Input Validation                                     │
│     ├─ Schema validation (Zod)                          │
│     ├─ SQL injection prevention                         │
│     └─ XSS protection                                   │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    Production Stack                      │
└─────────────────────────────────────────────────────────┘

┌──────────────┐
│   Vercel     │  ← Next.js App Deployment
│              │  ← Edge Functions
│              │  ← API Routes
└──────┬───────┘
       │
       │  ┌────────────┐
       └─►│  Convex    │  ← Database & Backend
          │            │  ← Cron Jobs
          │            │  ← Real-time Sync
          └────────────┘

Additional Services:
  ├─ Stripe (Billing)
  ├─ SendGrid (Email)
  ├─ Sentry (Error Tracking)
  └─ PostHog (Analytics)
```

---

**Last Updated:** 2025-12-30
**Version:** 1.0.0
**Maintained By:** B2B API Team
