/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CONFIGURE_X402_DISCRIMINATOR = new Uint8Array([
  229, 129, 8, 225, 242, 160, 132, 248,
]);

export function getConfigureX402DiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CONFIGURE_X402_DISCRIMINATOR,
  );
}

export type ConfigureX402Instruction<
  TProgram extends string = typeof GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountAgent extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountClock extends string | AccountMeta<string> =
    "SysvarC1ock11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAgent extends string
        ? WritableAccount<TAccountAgent>
        : TAccountAgent,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountClock extends string
        ? ReadonlyAccount<TAccountClock>
        : TAccountClock,
      ...TRemainingAccounts,
    ]
  >;

export type ConfigureX402InstructionData = {
  discriminator: ReadonlyUint8Array;
  agentId: string;
  enabled: boolean;
  paymentAddress: Address;
  acceptedTokens: Array<Address>;
  pricePerCall: bigint;
  serviceEndpoint: string;
};

export type ConfigureX402InstructionDataArgs = {
  agentId: string;
  enabled: boolean;
  paymentAddress: Address;
  acceptedTokens: Array<Address>;
  pricePerCall: number | bigint;
  serviceEndpoint: string;
};

export function getConfigureX402InstructionDataEncoder(): Encoder<ConfigureX402InstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["agentId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["enabled", getBooleanEncoder()],
      ["paymentAddress", getAddressEncoder()],
      ["acceptedTokens", getArrayEncoder(getAddressEncoder())],
      ["pricePerCall", getU64Encoder()],
      [
        "serviceEndpoint",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
    ]),
    (value) => ({ ...value, discriminator: CONFIGURE_X402_DISCRIMINATOR }),
  );
}

export function getConfigureX402InstructionDataDecoder(): Decoder<ConfigureX402InstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["agentId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["enabled", getBooleanDecoder()],
    ["paymentAddress", getAddressDecoder()],
    ["acceptedTokens", getArrayDecoder(getAddressDecoder())],
    ["pricePerCall", getU64Decoder()],
    [
      "serviceEndpoint",
      addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
    ],
  ]);
}

export function getConfigureX402InstructionDataCodec(): Codec<
  ConfigureX402InstructionDataArgs,
  ConfigureX402InstructionData
> {
  return combineCodec(
    getConfigureX402InstructionDataEncoder(),
    getConfigureX402InstructionDataDecoder(),
  );
}

export type ConfigureX402AsyncInput<
  TAccountAgent extends string = string,
  TAccountOwner extends string = string,
  TAccountClock extends string = string,
> = {
  /** Agent account with canonical PDA validation */
  agent?: Address<TAccountAgent>;
  /** Agent owner who can configure x402 settings */
  owner: TransactionSigner<TAccountOwner>;
  /** Clock sysvar for timestamp validation */
  clock?: Address<TAccountClock>;
  agentId: ConfigureX402InstructionDataArgs["agentId"];
  enabled: ConfigureX402InstructionDataArgs["enabled"];
  paymentAddress: ConfigureX402InstructionDataArgs["paymentAddress"];
  acceptedTokens: ConfigureX402InstructionDataArgs["acceptedTokens"];
  pricePerCall: ConfigureX402InstructionDataArgs["pricePerCall"];
  serviceEndpoint: ConfigureX402InstructionDataArgs["serviceEndpoint"];
};

export async function getConfigureX402InstructionAsync<
  TAccountAgent extends string,
  TAccountOwner extends string,
  TAccountClock extends string,
  TProgramAddress extends Address =
    typeof GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: ConfigureX402AsyncInput<TAccountAgent, TAccountOwner, TAccountClock>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ConfigureX402Instruction<
    TProgramAddress,
    TAccountAgent,
    TAccountOwner,
    TAccountClock
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    agent: { value: input.agent ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    clock: { value: input.clock ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.agent.value) {
    accounts.agent.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([97, 103, 101, 110, 116])),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
        getUtf8Encoder().encode(expectSome(args.agentId)),
      ],
    });
  }
  if (!accounts.clock.value) {
    accounts.clock.value =
      "SysvarC1ock11111111111111111111111111111111" as Address<"SysvarC1ock11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.agent),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.clock),
    ],
    data: getConfigureX402InstructionDataEncoder().encode(
      args as ConfigureX402InstructionDataArgs,
    ),
    programAddress,
  } as ConfigureX402Instruction<
    TProgramAddress,
    TAccountAgent,
    TAccountOwner,
    TAccountClock
  >);
}

export type ConfigureX402Input<
  TAccountAgent extends string = string,
  TAccountOwner extends string = string,
  TAccountClock extends string = string,
> = {
  /** Agent account with canonical PDA validation */
  agent: Address<TAccountAgent>;
  /** Agent owner who can configure x402 settings */
  owner: TransactionSigner<TAccountOwner>;
  /** Clock sysvar for timestamp validation */
  clock?: Address<TAccountClock>;
  agentId: ConfigureX402InstructionDataArgs["agentId"];
  enabled: ConfigureX402InstructionDataArgs["enabled"];
  paymentAddress: ConfigureX402InstructionDataArgs["paymentAddress"];
  acceptedTokens: ConfigureX402InstructionDataArgs["acceptedTokens"];
  pricePerCall: ConfigureX402InstructionDataArgs["pricePerCall"];
  serviceEndpoint: ConfigureX402InstructionDataArgs["serviceEndpoint"];
};

export function getConfigureX402Instruction<
  TAccountAgent extends string,
  TAccountOwner extends string,
  TAccountClock extends string,
  TProgramAddress extends Address =
    typeof GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: ConfigureX402Input<TAccountAgent, TAccountOwner, TAccountClock>,
  config?: { programAddress?: TProgramAddress },
): ConfigureX402Instruction<
  TProgramAddress,
  TAccountAgent,
  TAccountOwner,
  TAccountClock
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    agent: { value: input.agent ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    clock: { value: input.clock ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.clock.value) {
    accounts.clock.value =
      "SysvarC1ock11111111111111111111111111111111" as Address<"SysvarC1ock11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.agent),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.clock),
    ],
    data: getConfigureX402InstructionDataEncoder().encode(
      args as ConfigureX402InstructionDataArgs,
    ),
    programAddress,
  } as ConfigureX402Instruction<
    TProgramAddress,
    TAccountAgent,
    TAccountOwner,
    TAccountClock
  >);
}

export type ParsedConfigureX402Instruction<
  TProgram extends string = typeof GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Agent account with canonical PDA validation */
    agent: TAccountMetas[0];
    /** Agent owner who can configure x402 settings */
    owner: TAccountMetas[1];
    /** Clock sysvar for timestamp validation */
    clock: TAccountMetas[2];
  };
  data: ConfigureX402InstructionData;
};

export function parseConfigureX402Instruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedConfigureX402Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      agent: getNextAccount(),
      owner: getNextAccount(),
      clock: getNextAccount(),
    },
    data: getConfigureX402InstructionDataDecoder().decode(instruction.data),
  };
}
