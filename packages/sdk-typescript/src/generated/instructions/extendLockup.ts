/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const EXTEND_LOCKUP_DISCRIMINATOR = new Uint8Array([
  11, 43, 205, 79, 196, 188, 236, 222,
]);

export function getExtendLockupDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EXTEND_LOCKUP_DISCRIMINATOR,
  );
}

export type ExtendLockupInstruction<
  TProgram extends string = typeof GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountStakingAccount extends string | AccountMeta<string> = string,
  TAccountStakingConfig extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountStakingAccount extends string
        ? WritableAccount<TAccountStakingAccount>
        : TAccountStakingAccount,
      TAccountStakingConfig extends string
        ? ReadonlyAccount<TAccountStakingConfig>
        : TAccountStakingConfig,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type ExtendLockupInstructionData = {
  discriminator: ReadonlyUint8Array;
  newTier: number;
};

export type ExtendLockupInstructionDataArgs = { newTier: number };

export function getExtendLockupInstructionDataEncoder(): FixedSizeEncoder<ExtendLockupInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newTier", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: EXTEND_LOCKUP_DISCRIMINATOR }),
  );
}

export function getExtendLockupInstructionDataDecoder(): FixedSizeDecoder<ExtendLockupInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newTier", getU8Decoder()],
  ]);
}

export function getExtendLockupInstructionDataCodec(): FixedSizeCodec<
  ExtendLockupInstructionDataArgs,
  ExtendLockupInstructionData
> {
  return combineCodec(
    getExtendLockupInstructionDataEncoder(),
    getExtendLockupInstructionDataDecoder(),
  );
}

export type ExtendLockupAsyncInput<
  TAccountStakingAccount extends string = string,
  TAccountStakingConfig extends string = string,
  TAccountOwner extends string = string,
> = {
  stakingAccount?: Address<TAccountStakingAccount>;
  stakingConfig?: Address<TAccountStakingConfig>;
  owner: TransactionSigner<TAccountOwner>;
  newTier: ExtendLockupInstructionDataArgs["newTier"];
};

export async function getExtendLockupInstructionAsync<
  TAccountStakingAccount extends string,
  TAccountStakingConfig extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address =
    typeof GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: ExtendLockupAsyncInput<
    TAccountStakingAccount,
    TAccountStakingConfig,
    TAccountOwner
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ExtendLockupInstruction<
    TProgramAddress,
    TAccountStakingAccount,
    TAccountStakingConfig,
    TAccountOwner
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    stakingAccount: { value: input.stakingAccount ?? null, isWritable: true },
    stakingConfig: { value: input.stakingConfig ?? null, isWritable: false },
    owner: { value: input.owner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.stakingAccount.value) {
    accounts.stakingAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([115, 116, 97, 107, 105, 110, 103]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
      ],
    });
  }
  if (!accounts.stakingConfig.value) {
    accounts.stakingConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            115, 116, 97, 107, 105, 110, 103, 95, 99, 111, 110, 102, 105, 103,
          ]),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.stakingAccount),
      getAccountMeta(accounts.stakingConfig),
      getAccountMeta(accounts.owner),
    ],
    data: getExtendLockupInstructionDataEncoder().encode(
      args as ExtendLockupInstructionDataArgs,
    ),
    programAddress,
  } as ExtendLockupInstruction<
    TProgramAddress,
    TAccountStakingAccount,
    TAccountStakingConfig,
    TAccountOwner
  >);
}

export type ExtendLockupInput<
  TAccountStakingAccount extends string = string,
  TAccountStakingConfig extends string = string,
  TAccountOwner extends string = string,
> = {
  stakingAccount: Address<TAccountStakingAccount>;
  stakingConfig: Address<TAccountStakingConfig>;
  owner: TransactionSigner<TAccountOwner>;
  newTier: ExtendLockupInstructionDataArgs["newTier"];
};

export function getExtendLockupInstruction<
  TAccountStakingAccount extends string,
  TAccountStakingConfig extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address =
    typeof GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: ExtendLockupInput<
    TAccountStakingAccount,
    TAccountStakingConfig,
    TAccountOwner
  >,
  config?: { programAddress?: TProgramAddress },
): ExtendLockupInstruction<
  TProgramAddress,
  TAccountStakingAccount,
  TAccountStakingConfig,
  TAccountOwner
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    stakingAccount: { value: input.stakingAccount ?? null, isWritable: true },
    stakingConfig: { value: input.stakingConfig ?? null, isWritable: false },
    owner: { value: input.owner ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.stakingAccount),
      getAccountMeta(accounts.stakingConfig),
      getAccountMeta(accounts.owner),
    ],
    data: getExtendLockupInstructionDataEncoder().encode(
      args as ExtendLockupInstructionDataArgs,
    ),
    programAddress,
  } as ExtendLockupInstruction<
    TProgramAddress,
    TAccountStakingAccount,
    TAccountStakingConfig,
    TAccountOwner
  >);
}

export type ParsedExtendLockupInstruction<
  TProgram extends string = typeof GHOSTSPEAK_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    stakingAccount: TAccountMetas[0];
    stakingConfig: TAccountMetas[1];
    owner: TAccountMetas[2];
  };
  data: ExtendLockupInstructionData;
};

export function parseExtendLockupInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExtendLockupInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      stakingAccount: getNextAccount(),
      stakingConfig: getNextAccount(),
      owner: getNextAccount(),
    },
    data: getExtendLockupInstructionDataDecoder().decode(instruction.data),
  };
}
