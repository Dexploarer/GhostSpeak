{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "rm -rf node_modules packages/*/node_modules && bun install --force",
  "buildCommand": "rm -rf packages/web/.next && cd packages/sdk-typescript && npx tsup --no-dts && cd ../web && next build",
  "outputDirectory": "packages/web/.next"
}
