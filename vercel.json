{
  "framework": "nextjs",
  "buildCommand": "bun --cwd packages/plugin-ghostspeak run build && bunx turbo run build --filter=web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "bun install"
}
